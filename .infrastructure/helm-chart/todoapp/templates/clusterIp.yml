apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.common.name }}-service
  namespace: {{ .Values.common.namespace }}
spec:
  type: ClusterIP
  selector:
    app: todoapp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
