apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-nodeport
  namespace: {{ .Values.common.namespace }}
spec:
  type: NodePort
  selector:
    app: {{ .Values.common.name }}
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    nodePort: 30007
